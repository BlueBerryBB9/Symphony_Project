<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top">
  <div class="container-fluid">
    {# <a class="navbar-brand" href="#">Navbar</a> #}
    {# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> #}
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Accueil</a>
        </li>
        {% if app.user %}
            <a class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Actions
          </a>
            <ul class="dropdown-menu">
            {# {% if is_granted('ROLE_ADMIN') %} #}
              <li><a class="dropdown-item" href="{{path('app_article_index')}}">Articles</a></li>
              <li><a class="dropdown-item" href="{{path('app_comment_index')}}">Commentaires</a></li>
              <li><a class="dropdown-item" href="{{path('app_theme_index')}}">Thèmes</a></li>
              <li><hr class="dropdown-divider"></li>
            {# {% endif %} #}
              <li><a class="dropdown-item" href="{{path('app_article_new')}}">Écrire un article</a></li>
              <li><a class="dropdown-item" href="{{path('app_comment_new')}}">Écrire un comentaire</a></li>
              <li><a class="dropdown-item" href="{{path('app_theme_new')}}">Créer un Thème</a></li>
            </ul>
        </li>
        {% else %}
            <a class="nav-link" href="{{path('app_register')}}">S'enregistrer</a>
            <a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
        {% endif %}
        {# <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true">Disabled</a>
        </li> #}
      </ul>
      {# <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form> #}
    </div>
  </div>
</nav>